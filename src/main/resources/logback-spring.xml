<configuration debug="false" scan="false">

    <!-- Appender JSON para integração com Kibana -->
    <appender name="STDOUT-JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="com.sap.hcp.cf.logback.encoder.JsonEncoder"/>
    </appender>

    <!-- Appender padrão para console, mostrando correlationId -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - [correlationId=%X{correlationId}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger raiz -->
    <root level="${LOG_ROOT_LEVEL:-INFO}">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="STDOUT-JSON" />
    </root>

    <!-- Logger específico para SAP Cloud Logging (opcional) -->
    <logger name="com.sap.hcp.cf" level="INFO" />

    <!-- Logging específico de pacotes do Banheirinho -->
    <logger name="com.highwaytoiletfinder" level="DEBUG" />

    <!-- Logging de Hibernate e SQL -->
    <logger name="org.hibernate.SQL" level="DEBUG"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>

</configuration>