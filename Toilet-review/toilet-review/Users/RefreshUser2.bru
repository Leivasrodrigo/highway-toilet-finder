meta {
  name: RefreshUser2
  type: http
  seq: 10
}

post {
  url: //localhost:8080/auth/user
  body: json
  auth: none
}

body:json {
  {
    "command": "refresh",
    "refreshToken": "{{refreshToken2}}"
  }
}

script:post-response {
  bru.setEnvVar("jwtToken2", res.getBody().token);
  bru.setEnvVar("refreshToken2", res.getBody().refreshToken);
}
