meta {
  name: LoginUser3
  type: http
  seq: 14
}

post {
  url: //localhost:8080/auth/user
  body: json
  auth: none
}

body:json {
  {
    "command": "login",
    "email": "jean.lima@example.com",
    "password": "senhaSegura123"
  }
}

script:post-response {
  bru.setEnvVar("userId3", res.getBody().id);
  bru.setEnvVar("jwtToken3", res.getBody().token);
  bru.setEnvVar("refreshToken3", res.getBody().refreshToken);
  
}
