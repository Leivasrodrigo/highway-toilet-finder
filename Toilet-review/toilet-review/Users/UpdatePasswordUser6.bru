meta {
  name: UpdatePasswordUser6
  type: http
  seq: 22
}

post {
  url: //localhost:8080/api/users
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{jwtToken6}}
}

body:json {
  {
    "command": "updatePassword",
    "password": "NovaSenha123!"
  }
}

script:post-response {
  bru.setEnvVar("userId4", res.getBody().id);
  bru.setEnvVar("jwtToken4", res.getBody().token);
  bru.setEnvVar("refreshToken4", res.getBody().refreshToken);
  
}
