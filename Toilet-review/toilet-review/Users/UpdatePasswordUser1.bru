meta {
  name: UpdatePasswordUser1
  type: http
  seq: 4
}

post {
  url: //localhost:8080/api/users
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{jwtToken1}}
}

body:json {
  {
    "command": "updatePassword",
    "id": "{{userId1}}",
    "currentPassword": "senhaSegura123",
    "password": "senhaSegura234"
  }
}

script:post-response {
  bru.setEnvVar("userId1", res.getBody().id);
  
}
