meta {
  name: RefreshUser6
  type: http
  seq: 20
}

post {
  url: https://highway-toilet-backend-403782762727.us-central1.run.app/auth/user
  body: json
  auth: none
}

body:json {
  {
    "command": "refresh",
    "refreshToken": "{{refreshToken6}}"
  }
}

script:post-response {
  bru.setEnvVar("jwtToken6", res.getBody().token);
  bru.setEnvVar("refreshToken6", res.getBody().refreshToken);
}
