meta {
  name: LoginUser2
  type: http
  seq: 9
}

post {
  url: //localhost:8080/auth/user
  body: json
  auth: none
}

body:json {
  {
    "command": "login",
    "email": "pereira.getulio@example.com",
    "password": "senhaSegura123"
  }
}

script:post-response {
  bru.setEnvVar("userId2", res.getBody().id);
  bru.setEnvVar("jwtToken2", res.getBody().token);
  bru.setEnvVar("refresh2", res.getBody().refreshToken);
  
  
  
}
