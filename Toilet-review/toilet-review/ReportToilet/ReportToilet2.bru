meta {
  name: ReportToilet2
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/toilets
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{jwtToken2}}
}

body:json {
  {
    "command": "report",
    "id": "{{toiletId2}}",
    "userId": "{{userId2}}"
  }
}
