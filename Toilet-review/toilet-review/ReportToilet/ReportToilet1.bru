meta {
  name: ReportToilet1
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/toilets
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{jwtToken1}}
}

body:json {
  {
    "command": "report",
    "id": "{{toiletId2}}",
    "userId": "{{userId1}}"
  }
}
