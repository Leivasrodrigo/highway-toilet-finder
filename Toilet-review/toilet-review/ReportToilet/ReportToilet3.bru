meta {
  name: ReportToilet3
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/toilets
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{jwtToken3}}
}

body:json {
  {
    "command": "report",
    "id": "{{toiletId2}}",
    "userId": "{{userId3}}"
  }
}
