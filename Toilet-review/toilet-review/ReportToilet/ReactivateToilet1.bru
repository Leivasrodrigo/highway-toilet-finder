meta {
  name: ReactivateToilet1
  type: http
  seq: 6
}

post {
  url: http://localhost:8080/api/toilets
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{jwtToken1}}
}

body:json {
  {
    "command": "reactivate",
    "id": "{{toiletId2}}",
    "userId": "{{userId1}}"
  }
}
