meta {
  name: CreateReview2
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/reviews
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{jwtToken2}}
}

body:json {
  {
    "command": "create",
    "toiletId": "{{toiletId1}}",
    "ratingCleanliness": 2,
    "ratingMaintenance": 3,
    "comment": "Bem sujo, mas conservado."
  }
}

script:post-response {
  bru.setEnvVar("reviewId2", res.getBody().id);
}
