meta {
  name: CreateReview1
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/reviews
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{jwtToken1}}
}

body:json {
  {
    "command": "create",
    "toiletId": "{{toiletId2}}",
    "ratingCleanliness": 3,
    "ratingMaintenance": 5,
    "comment": "Limpeza razoável, mas bem conservado."
  }
}

script:post-response {
  bru.setEnvVar("reviewId1", res.getBody().id);
}
